<?php 
$tr = Application_Form_FrmLanguages::getCurrentlanguage();
echo $this->headTitle($tr->translate('COMMISSION'));
 $frm = $this->frm_cancel;
 $edit_sale = $tr->translate("EDIT");
 $urlEdit = $this->baseUrl()."/loan/comission/edit/id/";
 $urlReciept = $this->baseUrl()."/report/paramater/commissionreceipt/id/";
?>
<script>
	dojo.require("dijit.form.DateTextBox");
</script>
<form id="list" name="list" action="" dojoType="dijit.form.Form" method="post">   
	<table>	
		<tr>
			<td><?php echo $frm->getElement('adv_search');?></td>
			<td><?php echo $frm->getElement('branch_id_search');?></td>
			<td><?php echo $frm->getElement('staff_id');?></td>
			<td><?php echo $frm->getElement('from_date_search');?></td>
			<td><?php echo $frm->getElement('to_date_search');?></td>
			<td><button iconclass="dijitIconSearch" dojoType="dijit.form.Button" showLabel="true" type="submit"><?php echo $tr->translate("SEARCH");?></button>
			</td>
		</tr>
	</table>
	<?php echo $this->list;?>
</form>
<script>

var row=0;
var url="";
$(function(){
	$.contextMenu({
		selector: '.context-menu-one', 
		callback: function(key, options) {
		
			var m = "clicked: " + key;
			if(key=="edit"){
				url='<?php echo $urlEdit;?>';
			}else if(key=="receipt"){
				url='<?php echo $urlReciept;?>';
			}
			
			gotoAction();
			//window.console && console.log(m) || alert(m); 
		},
		items: {
			"edit": {name: "<?php echo $edit_sale;?>", icon: "edit", accesskey: "e"},
			"receipt": {name: "<?php echo $tr->translate("RECEIPT");?>", icon: "fa-money", accesskey: "h"},
		}
	});
});
function setrowdata(index){
	row = index;
}

var recordid ='';
function gotoAction(){
	 window.open(url+row, '_blank');
}
</script>